package sg.edu.nus.config;

import org.springframework.context.annotation.Configuration;

/*
*   AUTHOR: Amelia
*   PURPOSE: Generate password hash for mock data
*/
// @Configuration
public class PasswordHashGenerator {

    // @Bean
    // public CommandLineRunner generatePasswordHash(PasswordEncoder passwordEncoder) {
    //     return args -> {
    //         String passwordRaw = "Password@123";
    //         String passwordHash = passwordEncoder.encode(passwordRaw);
    //         System.out.println("BCrypt hash for " + passwordRaw + ":");
    //         System.out.println(passwordHash);
    //     };
    // }
    
}
